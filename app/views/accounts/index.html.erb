<div class="container">
    <div class="panel panel-default">
    <div class="panel-body">Balance : <%= @balance%> </div>
    <div class="panel-body">Debt :  <%= @debt%> </div>
    <div class="panel-body">Total: <%= Account.sum(:amount)%></div>
  </div>
</div>

<h2>Records</h2>

<div class="table-responsive">          
    <table class="table">

      <thead>
        <tr>
          <th>Title</th>
          <th>Date</th>
          <th>Amount</th>
        </tr>
      </thead>

        <tbody>

          <% @records.each do |record| %>
            <tr>
              <td><%= record.title %></td>
              <td><%= record.date %></td>
              <td><%= record.amount %></td>

              <td><%= link_to 'Edit', edit_account_path(record), class: 'btn btn-sm btn-success' %></td>
                <td>                    
                  <%= link_to 'Delete', account_path(record), method: :delete, class: 'btn btn-sm btn-danger', data: {confirm: "Are you sure?"} %>
              </td>
          <% end %>

            </tr>
        </tbody>
    </table>
</div>


<div class = "text-center">
  <%= will_paginate @records %>
</div>